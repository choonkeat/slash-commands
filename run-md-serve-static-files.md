Run `npx @choonkeat/md-serve` with **no arguments and no env overrides**; let md-serve pick its defaults. Inventing a port number is wrong.

The server is long-running -- leave it running in the background (the harness will auto-background it). Do not ask when to stop it.
